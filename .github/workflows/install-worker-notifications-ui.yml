name: Install worker notifications UI

on:
  push:
    branches: [main]
    paths:
      - 'bin/install-worker-notifications-ui.php'
      - 'deploy/worker-notifications-panel.php'
      - 'public/assets/worker-notifications.js'
      - 'public/assets/worker-notifications.css'
      - '.github/workflows/install-worker-notifications-ui.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: install-worker-notifications-ui-main
  cancel-in-progress: false

jobs:
  install:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none

      - name: Install notification settings in admin template
        run: php bin/install-worker-notifications-ui.php

      - name: Validate generated template
        run: php -l public/index.php

      - name: Commit generated template
        run: |
          if git diff --quiet -- public/index.php; then
            echo 'Worker notifications UI is already installed.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add public/index.php
          git commit -m 'Connect worker notification settings dashboard'
          git push origin HEAD:main
